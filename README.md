# train_ticket

## What is it ?
This is a personal project to teach me the use of databases. 

### How it work ?
Good question

## dependences
This project depend of one librairy (for now) :
 - Ncurces
 To install all the librarai do on linux : ```sudo install libncurses5-dev libncursesw5-dev```

## Development
This repo use CMake. If you want to compil the code, do this :

```
mkdir build
cd build
cmake ..
cmake --build .
```

then run app.exe to execute the programm.
